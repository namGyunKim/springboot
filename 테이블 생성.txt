create table members(
id varchar(50),
password varchar(50),
constraint memberentity__pk primary key(id)
);


create table article (
id number generated by default as identity,
content varchar(255),
title varchar(255),
constraint article__pk primary key(id)
    );

create table coffees (
    id number generated by default as identity,
    title varchar(255),
    content varchar(255),
    price number(10),
    constraint coffees__pk primary key(id)
);

create table ade (
    id number generated by default as identity,
    title varchar(255),
    content varchar(255),
    price number(10),
    constraint ade__pk primary key(id)
);

create table tea (
    id number generated by default as identity,
    title varchar(255),
    content varchar(255),
    price number(10),
    constraint tea__pk primary key(id)
);

create table latte (
    id number generated by default as identity,
    title varchar(255),
    content varchar(255),
    price number(10),
    constraint latte__pk primary key(id)
);